Template.navigationMap.helpers({
	test: function() {

		// map option
		// var oprtion = {
		// 	center: {
		// 		lat: Session.get('lat');
		// 		lng: Session.get('lon');
		// 	},
		// 	zoom: 10
		// }

		// element = document.getElementById('map-canvas');

		// map = new google.maps.Maps(element, options);
		return this.title;
	}
});
